package BasicAlgos.permutation;

import java.lang.reflect.Array;
import java.util.Arrays;
import java.util.List;
import java.util.Stack;

public class GenPermutationSeq {
    /**
     * @param src
     * @return
     */
    public List<List<Integer>> genSeq1 (int[] src) {
        for (int i = 0; i < src.length; i++) {

        }
        return null;
    }

    void permutateion (List<List<Integer>> res, int[] src, int start) {
        if (start != src.length - 1) {
            Arrays.asList(null);
        }

    }

    public static void main(String[] args) {
        int[] a = new int[12];
        Arrays.asList(null);
        GenPermutationSeq s = new GenPermutationSeq();
        var res = s.genSeq1(new int[]{1,2,3,4,5});
    }
}
